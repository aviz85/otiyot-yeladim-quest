/* newsroom.js — חדר הכתבים
   דובי הכתב (מדבר רק בכותרות), מרדכי החתול ששומר על עמוד 1 מתחת לארון,
   ופתרון: טונה מהמקרר ← מרדכי זז ← לוקחים את העמוד.
   Layout contract: see DESIGN.md "### newsroom" + newsroom_bg.js.
*/
'use strict';

registerRoom({
  id:'newsroom',
  name:'חדר הכתבים',
  music:'newsroom',
  bg:(ctx,t)=>BG.newsroom(ctx,t),
  walkY:158, minX:24, maxX:300,

  npcs:()=>{
    const a=[{s:'dovi', x:180, y:154, flip:false}];
    if(!G.f.cat_fed) a.push({s:'cat', x:262, y:152, flip:true});
    return a;
  },

  hotspots:[
    /* ---------- דובי הכתב ---------- */
    {name:'דובי הכתב', x:166,y:112,w:28,h:44, wx:150,
      look:'דובי, הכתב הוותיק. כובע עיתונאים, פנקס ביד, ועיניים שמחפשות כותרת גם בארוחת צהריים.',
      use: async()=>{
        const f=G.f;
        if(f.got1){
          await G.say('dovi','״סוף טוב לפרשת הארון: הילד נעם חילץ את העמוד — האומה נושמת לרווחה!״');
          await G.say('noam','דובי, אפשר פעם אחת סתם להגיד ״כל הכבוד״?');
          await G.say('dovi','״כתב מסרב להתרגש. ׳כל הכבוד׳ — כך מוסרים מקורות בחדר.״');
          await G.say('noam','אני אקח את זה כמחמאה. בכותרת ראשית.');
        } else if(f.cat_fed){
          await G.say('dovi','״דרמה בחדר הכתבים: החתול נטש את עמדתו — העמוד מתחת לארון חשוף לחלוטין!״');
          await G.say('noam','זאת אומרת שאני יכול פשוט לקחת אותו עכשיו?');
          await G.say('dovi','״כן — מאשרים גורמים רשמיים (זה אני).״');
        } else if(f.knows_tuna){
          await G.say('dovi','״תזכורת מערכת: מרדכי זז אך ורק בשביל טונה. טונה, ורק טונה!״');
          if(G.has('tuna')) await G.say('noam','כן כן, והטונה כבר אצלי. הולך לחתול!');
          else await G.say('noam','כן כן, שמעתי. מקרר. מטבחון. טונה. אני על זה.');
          await G.say('dovi','״הילד על זה — הציבור עוצר את נשימתו.״');
        } else {
          await G.say('dovi','״מבזק: ילד לא מוכר נכנס לחדר הכתבים — ההמשך יסופר!״');
          await G.say('noam','שלום, אני נעם. אתה תמיד מדבר בכותרות?');
          await G.say('dovi','״כתב ותיק נשבע: מאז שנת תשמ״ז — רק כותרות!״');
          await G.say('noam','זה לא מעייף?');
          await G.say('dovi','״ילד מטיל ספק בדרך חיים של כתב — הציבור המום!״');
          const c=await G.choice([
            'ראית עמוד מהכתבה שנעלמה?',
            'למה מרדכי יושב שם ליד הארון?',
            'מה הכותרת של מחר?'
          ]);
          if(c===0){
            await G.say('dovi','״בלעדי: דף לבן נגרר על ידי חשוד פרוותי אל מתחת לארון!״');
            await G.say('noam','מרדכי?! ידעתי! חתול עם פרצוף כזה תמים — תמיד אשם.');
            await G.say('dovi','״החשוד שומר על זכות המיאו-שתיקה.״');
          } else if(c===1){
            await G.say('dovi','״תעלומה: חתול המערכת יושב על אותה נקודה כבר שעתיים — מומחים מודאגים!״');
            await G.say('noam','חתולים לא יושבים סתם. הוא שומר על משהו!');
            await G.say('dovi','״הילד פיצח: מתחת לארון — דף! החשוד יושב על ההוכחה!״');
          } else {
            await G.say('dovi','״מחר: שמש. ואולי לא. העיתון מתנצל מראש.״');
            await G.say('noam','זו הכותרת? באמת?');
            await G.say('dovi','״קוראים דורשים ודאות — מזג האוויר מסרב להגיב.״');
          }
          await G.say('dovi','״ומהמטבחון נמסר: מרדכי מוכן לזוז מכל מקום — תמורת טונה. טונה, ורק טונה!״');
          G.f.knows_tuna=true;
          if(G.has('tuna')) await G.say('noam','טונה! רגע... יש לי כבר טונה בכיס! מרדכי, אני בא!');
          else await G.say('noam','טונה! אולי יש במקרר במטבחון. תודה דובי!');
          await G.say('dovi','״הילד יוצא לדרך — עדכונים בהמשך.״');
          autosave();
        }
      }},

    /* ---------- עמוד 1 מתחת לארון ---------- */
    {name:'פינת דף מתחת לארון', x:252,y:146,w:16,h:6, wx:240,
      visible: ()=>!G.f.got1,
      look: async()=>{
        if(!G.f.cat_fed) await G.say('noam','פינה של דף לבן מציצה מתחת לארון! וגם פינה של חתול. הפינה של החתול נראית פחות ידידותית.');
        else await G.say('noam','הנה הוא! עמוד מהכתבה, מציץ מתחת לארון בלי שום חתול מסביב!');
      },
      use: async()=>{
        if(!G.f.cat_fed){
          await G.say('noam','סליחה, מרדכי... אני רק אקח את הדף הקטן הזה ו...');
          G.sfx('meow');
          await G.say('cat','חְסססס! (תרגום: הדף הזה שלי. חתמתי עליו. בשערות.)');
          await G.say('noam','אוקיי, אוקיי! מוריד את היד!');
          if(G.f.knows_tuna) await G.say('noam','דובי אמר שאתה זז רק בשביל טונה. אחזור עם טונה — אל תלך לשום מקום. כאילו שהיית הולך.');
          else await G.say('noam','חתול עם עקרונות. חייב לגלות מה משכנע אותו... אולי דובי יודע משהו.');
        } else {
          G.add('page1'); G.f.got1=true;
          await G.say('noam','עמוד 1! קצת שערות חתול, קצת קמטים, אבל כולו כאן!');
          await G.say('noam','נשארו עוד שניים. אני עיתונאי במשימה!');
          autosave();
        }
      },
      items:{
        tuna: async()=>{
          if(G.f.cat_fed){ await G.say('noam','החתול כבר הלך. הדף לא אוכל טונה. נראה לי.'); return; }
          await feedMordechai();
        }
      }},

    /* ---------- מרדכי החתול ---------- */
    {name:'מרדכי החתול', x:250,y:136,w:26,h:18, wx:236,
      visible: ()=>!G.f.cat_fed,
      look:'מרדכי החתול. יושב על משמרתו כמו שומר בבנק, רק עם יותר שערות ופחות מוטיבציה.',
      use: async()=>{
        G.sfx('meow');
        await G.say('cat','מיאאאו. (תרגום: שלום. אני יושב פה. לנצח.)');
        await G.say('noam','מרדכי, זוז רגע, אני רק צריך את הדף שמתחתיך...');
        await G.say('cat','מררר. (תרגום: הגש בקשה בכתב. אשב עליה גם.)');
        if(G.f.knows_tuna) await G.say('noam','טוב, הבנתי את הרמז של דובי. בלי טונה — אין דף.');
        else await G.say('noam','הוא לא זז. אולי דובי יודע מה משגע את החתול הזה.');
      },
      items:{
        tuna: async()=>{ await feedMordechai(); },
        cookie: async()=>{
          G.sfx('meow');
          await G.say('cat','מְנֶה. (תרגום: עוגייה? אני חתול, לא סבתא.)');
          await G.say('noam','טעות שלי. לחתול הזה יש תפריט מסודר, כנראה.');
        }
      }},

    /* ---------- ריהוט ופרטים ---------- */
    {name:'שולחן כתבים 1', x:44,y:100,w:70,h:46, wx:80,
      look:'מחשב עתיק עם מסך ירוק מהבהב. הוא חושב כבר עשר דקות על הקובץ ״כתבה_סופית_סופית_באמת3״.',
      use:'הקשתי על מקש. המחשב נאנח. עזבתי אותו בשקט.'},

    {name:'מפת הסקופים', x:50,y:30,w:50,h:34, wx:75,
      look:'״מפת הסקופים״ — נעצים אדומים בכל הארץ. יש גם נעץ אחד בים התיכון. כנראה כתבת הדגים.',
      use:'לא נוגע בנעצים. פעם מישהו הזיז נעץ ודובי כתב על זה שלוש כתבות.'},

    {name:'שולחן כתבים 2', x:154,y:100,w:50,h:46, wx:210,
      look:'שולחן של כתב אמיתי: שבע כוסות קפה. שש ריקות, ואחת מגדלת משהו ירוק. אולי זה הסקופ הבא.',
      use:'אני לא מרים את הכוס השביעית. יש דברים שילד לא צריך לראות.'},

    {name:'החלון', x:162,y:30,w:40,h:38, wx:182,
      look:'החלון פתוח לרווחה והרוח מדפדפת בניירות של כולם. מכאן רואים את הגג של הבניין.',
      use: async()=>{
        if(G.f.got2){ await G.say('noam','מהחלון הזה עף עמוד 2 עד לקן של נחום. סוף טוב — העמוד אצלי, והיונה קיבלה גרעינים.'); return; }
        await G.say('noam','איזו רוח! מכאן בטח עף החוצה לפחות עמוד אחד...');
        if(!G.f.knows_pigeon){
          await G.say('noam','רגע — רואים מפה את הגג! ויש שם קן של יונה. שווה לטפס לבדוק!');
          G.f.knows_pigeon=true;
        } else {
          await G.say('noam','הגג מחכה לי. והיונה כנראה גם.');
        }
      }},

    {name:'מכונת הכתיבה', x:206,y:110,w:28,h:36, wx:220,
      look:'״המוזיאון של דובי״ — מכונת כתיבה מצוחצחת. דובי מנקה אותה כל בוקר ואומר לה ״בוקר טוב, גברת״.',
      use:'הקשתי אות אחת. הקליק היה כל כך מספק שכמעט כתבתי רומן. אבל יש לי דדליין.'},

    {name:'הארון הגדול', x:240,y:60,w:50,h:70, wx:230,
      look:'ארון ענק מלא תיקיות. מודבק עליו פתק ״לסדר בהקדם!״ — הפתק כבר הצהיב מרוב הקדם.',
      use: async()=>{
        if(!G.f.got1 && !G.f.cat_fed) await G.say('noam','משהו לבן מציץ מתחת לארון... והחתול הזה יושב עליו כמו על כיסא מנהלים.');
        else if(!G.f.got1) await G.say('noam','הדף עדיין מציץ מתחת לארון. רק להתכופף ולקחת!');
        else await G.say('noam','הארון כבד מדי בשביל להזיז, ובכל מקרה — מה שחיפשתי כבר אצלי.');
      }},

    {name:'ערימות ניירות', x:10,y:144,w:24,h:14, wx:40,
      look:'ערימות ניירות ליד הדלת. פעם זה היה יער. עכשיו זה טיוטות של דובי — כותרות בלי כתבות.',
      use:'התחלתי לדפדף. ״ילד מדפדף בערימה — לא נמצא דבר!״ — טוב, זה מדבק.'},

    /* ---------- יציאות ---------- */
    {name:'דלת ללובי', x:8,y:72,w:28,h:70, wx:30,
      look:'הדלת חזרה ללובי. שם גילה יודעת הכול על כולם, כולל דברים שעוד לא קרו.',
      use: async()=>{ G.go('lobby',240); }},

    {name:'דלת חדר הישיבות', x:120,y:72,w:28,h:70, wx:134,
      look:'״חדר ישיבות״. המקום שבו רעיונות טובים נכנסים, ופרוטוקולים ארוכים יוצאים.',
      use: async()=>{ G.go('meeting',50); }},

    {name:'דלת הגרפיקה', x:292,y:72,w:24,h:70, wx:296,
      look:'הדלת לחדר הגרפיקה של יוסי. שומעים מבפנים מישהו מתווכח בקול רם עם פונט.',
      use: async()=>{ G.go('design',270); }},
  ],
});

/* האכלת מרדכי — משותף לחתול ולפינת הדף */
async function feedMordechai(){
  await G.say('noam','מרדכי... תראה מה יש לי כאן... טונה!');
  G.sfx('meow');
  await G.say('cat','מיאו?! מיאו-מיאו!! (תרגום: טונה?! כל העמודים שבעולם לא שווים קופסה אחת!)');
  G.del('tuna'); G.f.cat_fed=true;
  await G.say('cat','מררר-פררר... (תרגום: העמוד שלך, ילד. היה כבוד לשמור עליו.)');
  await G.say('noam','והוא הלך עם הטונה כאילו כלום. חתולים — נאמנים עד הקופסה הראשונה.');
  autosave();
}
